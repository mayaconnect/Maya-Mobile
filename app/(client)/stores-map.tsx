/**
 * Maya Connect V2 — Stores Map Screen (Client)
 *
 * Route wrapper that delegates to the platform-specific StoresMapView component.
 * - Native (iOS/Android): uses react-native-maps
 * - Web: shows a fallback message
 */
export { default } from '../../src/components/client/StoresMapView';

