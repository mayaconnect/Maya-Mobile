name: Deploy to TestFlight & Google Play

on:
  # Déploiement automatique sur push vers master
  push:
    branches:
      - master
      - main

  # Déploiement manuel depuis l'interface GitHub
  workflow_dispatch:
    inputs:
      platform:
        description: 'Platform to deploy'
        required: true
        type: choice
        options:
          - both
          - android
          - ios
        default: 'both'

jobs:
  # ==========================================
  # JOB ANDROID - Build et Deploy sur Google Play
  # ==========================================
  deploy-android:
    name: 🤖 Android → Google Play
    env:
      EXPO_PUBLIC_API_BASE_URL: ${{ secrets.EXPO_PUBLIC_API_BASE_URL }}
    if: |
      github.event_name == 'push' ||
      (github.event_name == 'workflow_dispatch' &&
       (github.event.inputs.platform == 'android' || github.event.inputs.platform == 'both'))
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 📦 Install dependencies (excluding dev dependencies for Release)
        run: npm ci --omit=dev

      - name: 💾 Backup Fastlane configuration
        run: |
          mkdir -p /tmp/fastlane-backup
          cp -r android/fastlane /tmp/fastlane-backup/ || echo "No fastlane folder to backup"

      - name: 📝 Create .env file for API URL
        run: |
          if [ -z "${{ secrets.EXPO_PUBLIC_API_BASE_URL }}" ]; then
            echo "⚠️ WARNING: EXPO_PUBLIC_API_BASE_URL secret is not set!"
            echo "The app will use the default API URL from the code."
          else
            echo "EXPO_PUBLIC_API_BASE_URL=${{ secrets.EXPO_PUBLIC_API_BASE_URL }}" > .env
            echo "✅ .env file created with API URL"
            # Verify the file was created (without showing the actual URL)
            if [ -f ".env" ]; then
              echo "✅ .env file exists"
              echo "API URL configured: EXPO_PUBLIC_API_BASE_URL=***"
            else
              echo "❌ ERROR: .env file was not created"
              exit 1
            fi
          fi

      - name: 🔨 Generate Android native folder
        run: |
          # Export the variable so it's available during prebuild
          export EXPO_PUBLIC_API_BASE_URL="${{ secrets.EXPO_PUBLIC_API_BASE_URL }}"
          npx expo prebuild --platform android --clean

      - name: 📋 Restore Fastlane configuration
        run: |
          cp -r /tmp/fastlane-backup/fastlane android/
          echo "Fastlane configuration restored"
          ls -la android/fastlane/

      - name: 🔧 Configure release signing in build.gradle
        run: |
          echo "🔧 Configuring release signing in build.gradle..."
          BUILD_GRADLE="android/app/build.gradle"
          
          if [ ! -f "$BUILD_GRADLE" ]; then
            echo "❌ ERROR: build.gradle not found. expo prebuild may have failed."
            exit 1
          fi
          
          # Create backup
          cp "$BUILD_GRADLE" "$BUILD_GRADLE.backup"
          
          # Check if signingConfigs.release already exists
          if grep -q "signingConfigs.release" "$BUILD_GRADLE" || (grep -A 10 "signingConfigs {" "$BUILD_GRADLE" | grep -q "release {"); then
            echo "✅ signingConfigs.release already exists"
          else
            # Add release config after debug config in signingConfigs block
            # Find the closing brace of signingConfigs block and insert release config before it
            awk '
              /signingConfigs \{/ { 
                print
                in_signing = 1
                next
              }
              in_signing && /^    \}/ {
                print "        release {"
                print "            if (project.hasProperty(\"MAYA_UPLOAD_STORE_FILE\")) {"
                print "                storeFile file(MAYA_UPLOAD_STORE_FILE)"
                print "                storePassword MAYA_UPLOAD_STORE_PASSWORD"
                print "                keyAlias MAYA_UPLOAD_KEY_ALIAS"
                print "                keyPassword MAYA_UPLOAD_KEY_PASSWORD"
                print "            }"
                print "        }"
                print
                in_signing = 0
              }
              { print }
            ' "$BUILD_GRADLE" > "$BUILD_GRADLE.tmp" && mv "$BUILD_GRADLE.tmp" "$BUILD_GRADLE"
            echo "✅ Added signingConfigs.release"
          fi
          
          # Ensure release buildType uses signingConfigs.release instead of debug
          # First, check if release block uses debug (this is the problem we're fixing)
          if grep -A 10 "release {" "$BUILD_GRADLE" | grep -q "signingConfig signingConfigs\.debug"; then
            echo "⚠️  WARNING: Release buildType is using debug signing! Fixing..."
            # Replace debug signingConfig with release in the release block
            sed -i '/release {/,/^    }/s/signingConfig signingConfigs\.debug/signingConfig signingConfigs.release/g' "$BUILD_GRADLE"
            echo "✅ Fixed: Replaced debug signing with release signing"
          elif grep -A 10 "release {" "$BUILD_GRADLE" | grep -q "signingConfig signingConfigs.release"; then
            echo "✅ Release buildType already uses signingConfigs.release"
          else
            # No signingConfig found in release block, add it
            echo "⚠️  No signingConfig found in release block, adding signingConfigs.release"
            sed -i '/release {/a\            signingConfig signingConfigs.release' "$BUILD_GRADLE"
            echo "✅ Added signingConfig signingConfigs.release to release buildType"
          fi
          
          # Final verification - ensure release does NOT use debug
          if grep -A 10 "release {" "$BUILD_GRADLE" | grep -q "signingConfig signingConfigs\.debug"; then
            echo "❌ ERROR: Release buildType still uses debug signing after configuration!"
            echo "Build.gradle content around release block:"
            grep -A 10 "release {" "$BUILD_GRADLE"
            exit 1
          else
            echo "✅ Verification passed: Release buildType uses release signing (not debug)"
          fi
          
          echo "✅ Release signing configuration complete"

      - name: 🔧 Set gradlew permissions
        working-directory: android
        run: |
          chmod +x gradlew
          echo "✅ Gradlew permissions set"

      - name: 🔧 Generate React Native Codegen artifacts
        working-directory: android
        run: |
          echo "Generating codegen artifacts for all modules..."
          # Generate codegen artifacts for all native modules, not just the app
          ./gradlew generateCodegenArtifactsFromSchema --stacktrace || {
            echo "⚠️ Codegen generation had issues, but continuing..."
            exit 0
          }
          echo "✅ Codegen artifacts generated"

      - name: 💎 Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.2'
          bundler-cache: true

      - name: 🔐 Setup Android keystore
        run: |
          set -e
          # Always use the keystore from GitHub secret (never use local file)
          echo "📦 Creating keystore from secret..."
          # Important: use printf instead of echo -n for better compatibility
          printf '%s' "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" | base64 -d > maya-release.keystore
          if [ ! -f maya-release.keystore ]; then
            echo "❌ ERROR: Failed to create keystore file"
            exit 1
          fi
          echo "✅ Keystore created successfully at project root"
          # Copy keystore to android/app/ for Gradle to find it easily
          mkdir -p android/app
          cp maya-release.keystore android/app/maya-release.keystore
          echo "✅ Keystore copied to android/app/"
          # Verify keystore SHA1 matches expected value
          echo "🔍 Verifying keystore SHA1..."
          SHA1=$(keytool -list -v -keystore android/app/maya-release.keystore -alias "${{ secrets.ANDROID_KEY_ALIAS }}" -storepass "${{ secrets.ANDROID_KEYSTORE_PASSWORD }}" 2>/dev/null | grep "SHA1:" | awk '{print $2}' || echo "")
          echo "Keystore SHA1: $SHA1"
          EXPECTED_SHA1="1C:3B:87:09:A5:5B:A3:74:A3:C2:7A:98:AF:23:06:82:C7:B1:AF:FD"
          if [ "$SHA1" = "$EXPECTED_SHA1" ]; then
            echo "✅ Keystore SHA1 matches expected value!"

          else
            echo "❌ ERROR: Keystore SHA1 does NOT match expected value!"
            echo "Expected: $EXPECTED_SHA1"
            echo "Got:      $SHA1"
            echo ""
            echo "This will cause Google Play to reject the AAB."
            echo "Please update the ANDROID_KEYSTORE_BASE64 secret with the correct keystore."
            echo "Use the script: scripts/verify-and-encode-keystore.ps1 to verify and encode the correct keystore."
            exit 1
          fi
          ls -lh maya-release.keystore android/app/maya-release.keystore

      - name: ⚙️ Create gradle.properties
        run: |
          cat << EOF > android/gradle.properties
          MAYA_UPLOAD_STORE_FILE=maya-release.keystore
          MAYA_UPLOAD_KEY_ALIAS=${{ secrets.ANDROID_KEY_ALIAS }}
          MAYA_UPLOAD_STORE_PASSWORD=${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          MAYA_UPLOAD_KEY_PASSWORD=${{ secrets.ANDROID_KEY_PASSWORD }}

          # Gradle settings
          org.gradle.jvmargs=-Xmx2048m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8
          org.gradle.parallel=true
          org.gradle.configureondemand=true
          android.useAndroidX=true
          android.enableJetifier=true

          # React Native / Hermes settings
          hermesEnabled=true
          newArchEnabled=true
          EOF
          echo "Gradle properties configured"

      - name: 🔑 Create Google Play service account JSON
        run: |
          mkdir -p android/fastlane
          echo '${{ secrets.GOOGLE_PLAY_SERVICE_ACCOUNT_JSON }}' > android/fastlane/google-play-service-account.json
          echo "Service account configured"

      - name: 🚀 Deploy to Google Play Internal Testing
        working-directory: android
        run: bundle exec fastlane internal
        env:
          SUPPLY_JSON_KEY: ./fastlane/google-play-service-account.json

      - name: 📤 Upload Android artifact
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: android-release-aab
          path: android/app/build/outputs/bundle/release/app-release.aab
          retention-days: 30

      - name: ✅ Android deployment success
        if: success()
        run: |
          echo "✅ Android app successfully deployed to Google Play Internal Testing!"
          echo "📱 Go to Google Play Console to test: https://play.google.com/console/"

  # ==========================================
  # JOB iOS - Build et Deploy sur TestFlight
  # ==========================================
  deploy-ios:
    name: 🍎 iOS → TestFlight
    env:
      _EXPO_PUBLIC_API_BASE_URL: ${{ vars._EXPO_PUBLIC_API_BASE_URL }}
    if: |
      github.event_name == 'push' ||
      (github.event_name == 'workflow_dispatch' &&
       (github.event.inputs.platform == 'ios' || github.event.inputs.platform == 'both'))
    runs-on: macos-15  # macOS 15 has Xcode 16.x

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 📦 Install dependencies (excluding dev dependencies for Release)
        run: npm ci --omit=dev

      - name: 🔧 Select Xcode 16.1
        run: |
          sudo xcode-select -s /Applications/Xcode_16.1.app/Contents/Developer
          xcodebuild -version

      - name: 🔨 Generate iOS native folder
        run: npx expo prebuild --platform ios --clean

      - name: 🔧 Fix iOS nullability issues
        run: |
          chmod +x scripts/fix-ios-nullability.sh
          bash scripts/fix-ios-nullability.sh

      - name: 🔧 Set iOS deployment target in Xcode project
        run: |
          # Find the Xcode project file dynamically
          PROJECT_FILE=$(find ios -name "project.pbxproj" -type f | head -n 1)
          if [ -n "$PROJECT_FILE" ]; then
            echo "📱 Found Xcode project at: $PROJECT_FILE"
            sed -i '' 's/IPHONEOS_DEPLOYMENT_TARGET = [^;]*/IPHONEOS_DEPLOYMENT_TARGET = 13.4/g' "$PROJECT_FILE"
            echo "✅ Set IPHONEOS_DEPLOYMENT_TARGET to 13.4"
            # Verify the change
            echo "🔍 Verification:"
            grep "IPHONEOS_DEPLOYMENT_TARGET" "$PROJECT_FILE" | head -n 3
          else
            echo "⚠️ Warning: Could not find Xcode project.pbxproj file"
            exit 1
          fi

      - name: 💎 Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.2'
          bundler-cache: true

      - name: 🧹 Clean Xcode Derived Data
        run: rm -rf ~/Library/Developer/Xcode/DerivedData

      - name: 🔧 Configure Podfile build settings
        working-directory: ios
        run: |
          echo "🔧 Adding custom build settings to Podfile post_install hook"

          # Insert custom build settings into the existing post_install hook
          sed -i '' '/post_install do |installer|/a\
          \  # Custom build settings\
          \  installer.pods_project.targets.each do |target|\
          \    target.build_configurations.each do |config|\
          \      config.build_settings["IPHONEOS_DEPLOYMENT_TARGET"] = "13.4"\
          \      config.build_settings["GCC_WARN_INHIBIT_ALL_WARNINGS"] = "YES"\
          \      config.build_settings["SWIFT_SUPPRESS_WARNINGS"] = "YES"\
          \    end\
          \  end\
          ' Podfile

          echo "✅ Podfile configured"

      - name: 📦 Install CocoaPods dependencies
        working-directory: ios
        run: |
          pod install
          echo "✅ CocoaPods dependencies installed"

      - name: 🔧 Set deployment target for all Pods
        run: |
          cd ios
          echo "Setting IPHONEOS_DEPLOYMENT_TARGET for all Pods to 13.4"
          find Pods -name "project.pbxproj" -type f | while read -r file; do
            sed -i '' 's/IPHONEOS_DEPLOYMENT_TARGET = [^;]*/IPHONEOS_DEPLOYMENT_TARGET = 13.4/g' "$file"
          done
          echo "✅ All Pods deployment targets updated"

      - name: 🔧 Disable Swift 6 strict concurrency checking
        run: |
          PROJECT_FILE=$(find ios -name "project.pbxproj" -type f | head -n 1)
          if [ -n "$PROJECT_FILE" ]; then
            echo "📱 Disabling strict concurrency checking in: $PROJECT_FILE"
            awk '/buildSettings = \{/ {print; print "\t\t\t\tSWIFT_STRICT_CONCURRENCY = minimal;"; next}1' "$PROJECT_FILE" > "$PROJECT_FILE.tmp"
            mv "$PROJECT_FILE.tmp" "$PROJECT_FILE"
            echo "✅ Swift strict concurrency checking disabled"
          fi

      - name: 🔐 Setup App Store Connect API Key
        run: |
          mkdir -p ios/fastlane/keys
          echo "${{ secrets.APP_STORE_CONNECT_KEY_BASE64 }}" | base64 -d > ios/fastlane/keys/AuthKey_${{ secrets.APP_STORE_CONNECT_KEY_ID }}.p8
          chmod 600 ios/fastlane/keys/AuthKey_${{ secrets.APP_STORE_CONNECT_KEY_ID }}.p8
          echo "App Store Connect API Key configured"

      - name: 📜 Setup Match certificates
        working-directory: ios
        run: bundle exec fastlane match appstore --readonly
        env:
          MATCH_PASSWORD: ${{ secrets.MATCH_PASSWORD }}
          MATCH_GIT_BASIC_AUTHORIZATION: ${{ secrets.MATCH_GIT_BASIC_AUTHORIZATION }}
          MATCH_GIT_URL: ${{ secrets.MATCH_GIT_URL }}
          FASTLANE_APPLE_APPLICATION_SPECIFIC_PASSWORD: ${{ secrets.FASTLANE_APPLE_APPLICATION_SPECIFIC_PASSWORD }}

      - name: 🚀 Build and Deploy to TestFlight
        working-directory: ios
        run: bundle exec fastlane beta
        env:
          APP_STORE_CONNECT_KEY_ID: ${{ secrets.APP_STORE_CONNECT_KEY_ID }}
          APP_STORE_CONNECT_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_ISSUER_ID }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
          MATCH_PASSWORD: ${{ secrets.MATCH_PASSWORD }}
          MATCH_GIT_BASIC_AUTHORIZATION: ${{ secrets.MATCH_GIT_BASIC_AUTHORIZATION }}
          MATCH_GIT_URL: ${{ secrets.MATCH_GIT_URL }}

      - name: 📤 Upload iOS artifact
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: ios-release-ipa
          path: ios/*.ipa
          retention-days: 30

      - name: ✅ iOS deployment success
        if: success()
        run: |
          echo "✅ iOS app successfully deployed to TestFlight!"
          echo "📱 Go to App Store Connect to test: https://appstoreconnect.apple.com/"

  # ==========================================
  # JOB NOTIFICATION - Résumé final
  # ==========================================
  notify-success:
    name: 📢 Deployment Summary
    needs: [deploy-android, deploy-ios]
    if: always()
    runs-on: ubuntu-latest

    steps:
      - name: 📊 Deployment Summary
        run: |
          echo "======================================"
          echo "🎉 DEPLOYMENT COMPLETED"
          echo "======================================"
          echo ""
          echo "Android Status: ${{ needs.deploy-android.result }}"
          echo "iOS Status: ${{ needs.deploy-ios.result }}"
          echo ""
          if [ "${{ needs.deploy-android.result }}" == "success" ]; then
            echo "✅ Android: https://play.google.com/console/"
          fi
          if [ "${{ needs.deploy-ios.result }}" == "success" ]; then
            echo "✅ iOS: https://appstoreconnect.apple.com/"
          fi
          echo ""
          echo "======================================"
