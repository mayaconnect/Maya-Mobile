name: Setup Match (One-Time iOS Certificates) - API Key Only

# ⚠️ Exécuter une seule fois pour initialiser Match avec API Key uniquement
# Pas besoin d'Apple ID / mot de passe / 2FA

on:
  workflow_dispatch:
    inputs:
      team_id:
        description: "Your Apple Team ID"
        required: true
        type: string

jobs:
  setup-match:
    name: 🔐 Initialize Match & Create iOS Certificates
    runs-on: macos-15  # macOS 15 has Xcode 16.x

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 📦 Install dependencies
        run: npm ci

      - name: 🗑️ Remove expo-dev-client (not needed for production builds)
        run: npm uninstall expo-dev-client

      - name: 🔧 Select Xcode 16.1
        run: sudo xcode-select -s /Applications/Xcode_16.1.app/Contents/Developer

      - name: 🔨 Generate iOS native folder
        run: npx expo prebuild --platform ios --clean

      - name: 🔧 Fix iOS nullability issues
        run: |
          chmod +x scripts/fix-ios-nullability.sh
          bash scripts/fix-ios-nullability.sh

      - name: 💎 Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.2'
          bundler-cache: true

      - name: 🔐 Create App Store Connect API Key files (p8 + api_key.json)
        run: |
          mkdir -p ios/fastlane/keys

          # 1) Write .p8 from base64 secret
          echo "${{ secrets.APP_STORE_CONNECT_KEY_BASE64 }}" | base64 -d > "ios/fastlane/keys/AuthKey_${{ secrets.APP_STORE_CONNECT_KEY_ID }}.p8"
          chmod 600 "ios/fastlane/keys/AuthKey_${{ secrets.APP_STORE_CONNECT_KEY_ID }}.p8"

          # 2) Create api_key.json for fastlane
          cat > ios/fastlane/keys/api_key.json << EOF
          {
            "key_id": "${{ secrets.APP_STORE_CONNECT_KEY_ID }}",
            "issuer_id": "${{ secrets.APP_STORE_CONNECT_ISSUER_ID }}",
            "key_filepath": "ios/fastlane/keys/AuthKey_${{ secrets.APP_STORE_CONNECT_KEY_ID }}.p8",
            "in_house": false
          }
          EOF

          echo "✅ API Key files created"

      - name: 📝 Create Matchfile (no Apple ID required)
        run: |
          cat << EOF > ios/fastlane/Matchfile
          git_url("${{ secrets.MATCH_GIT_URL }}")
          storage_mode("git")
          type("appstore")
          app_identifier(["com.maya.app"])
          team_id("${{ github.event.inputs.team_id }}")
          api_key_path("fastlane/keys/api_key.json")
          EOF
          echo "✅ Matchfile created"

      - name: 🎫 Initialize Match and Create Certificates (API Key)
        working-directory: ios
        run: |
          echo "🔐 Initializing Match with App Store Connect API Key..."

          bundle exec fastlane match appstore \
            --verbose \
            --force

          echo "✅ Match initialized successfully!"
          echo "✅ Certificates and profiles stored in: ${{ secrets.MATCH_GIT_URL }}"
        env:
          MATCH_PASSWORD: ${{ secrets.MATCH_PASSWORD }}
          MATCH_GIT_BASIC_AUTHORIZATION: ${{ secrets.MATCH_GIT_BASIC_AUTHORIZATION }}
          MATCH_GIT_URL: ${{ secrets.MATCH_GIT_URL }}
          APP_STORE_CONNECT_KEY_ID: ${{ secrets.APP_STORE_CONNECT_KEY_ID }}
          APP_STORE_CONNECT_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_ISSUER_ID }}

      - name: ✅ Success - Match is ready!
        run: |
          echo "======================================"
          echo "🎉 MATCH SETUP COMPLETED!"
          echo "======================================"
          echo ""
          echo "✅ iOS Certificates created"
          echo "✅ Provisioning Profiles created"
          echo "✅ Everything stored in: ${{ secrets.MATCH_GIT_URL }}"
          echo ""
          echo "📋 Next steps:"
          echo "1. The deploy workflow can now build iOS"
          echo "2. You can disable or delete this workflow"
          echo ""
          echo "======================================"
