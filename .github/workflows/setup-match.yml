name: Setup Match (One-Time iOS Certificates) - API Key Only

# ---------------------------------------------------------------------------
#  ⚠️  Ce workflow s'exécute UNE SEULE FOIS pour initialiser fastlane match
#      - Utilise UNIQUEMENT une App Store Connect API Key (pas d'Apple ID / 2FA)
#      - Crée les certificats iOS + provisioning profiles
#      - Stocke tout dans votre repo Git (MATCH_GIT_URL)
# ---------------------------------------------------------------------------

on:
  workflow_dispatch:
  

jobs:
  setup-match:
    name: 🔐 Initialize Match & Create iOS Certificates (API Key)
    runs-on: macos-15 # macOS 15 has Xcode 16.x

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      # ------------------------------
      #  Node & dépendances JS
      # ------------------------------
      - name: 📦 Install dependencies
        shell: bash
        run: |
          set -euo pipefail
          npm ci

      - name: 🗑️ Remove expo-dev-client (not needed for production builds)
        shell: bash
        run: |
          set -euo pipefail
          npm uninstall expo-dev-client

      # ------------------------------
      #  iOS native project (Expo)
      # ------------------------------
      - name: 🔧 Select Xcode 16.1
        shell: bash
        run: |
          set -euo pipefail
          sudo xcode-select -s /Applications/Xcode_16.1.app/Contents/Developer

      - name: 🔨 Generate iOS native folder
        shell: bash
        run: |
          set -euo pipefail
          npx expo prebuild --platform ios --clean

      - name: 🔧 Fix iOS nullability issues
        shell: bash
        run: |
          set -euo pipefail
          chmod +x scripts/fix-ios-nullability.sh
          bash scripts/fix-ios-nullability.sh

      # ------------------------------
      #  Ruby & fastlane
      # ------------------------------
      - name: 💎 Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.2"
          bundler-cache: true

      # ------------------------------
      #  App Store Connect API Key
      #  - Décode la clé .p8 depuis le secret BASE64
      #  - Génère ios/fastlane/keys/api_key.json au format attendu par fastlane
      # ------------------------------
      - name: 🔐 Create App Store Connect API Key files (p8 + api_key.json)
        shell: bash
        env:
          APP_STORE_CONNECT_KEY_ID: ${{ secrets.APP_STORE_CONNECT_KEY_ID }}
          APP_STORE_CONNECT_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_ISSUER_ID }}
          APP_STORE_CONNECT_KEY_BASE64: ${{ secrets.APP_STORE_CONNECT_KEY_BASE64 }}
        run: |
          set -euo pipefail

          mkdir -p ios/fastlane/keys

          # 1) Nettoyer et décoder le secret base64 (supprimer espaces et retours à la ligne)
          echo "🔍 Decoding App Store Connect API Key..."
          CLEAN_BASE64=$(echo "${APP_STORE_CONNECT_KEY_BASE64}" | tr -d '[:space:]')
          
          # 2) Écrire le fichier .p8 à partir du secret base64 nettoyé
          p8_path="ios/fastlane/keys/AuthKey_${APP_STORE_CONNECT_KEY_ID}.p8"
          echo "${CLEAN_BASE64}" | base64 -d > "${p8_path}"
          chmod 600 "${p8_path}"
          
          # 3) Vérifier que le fichier .p8 est valide
          if [ ! -s "${p8_path}" ]; then
            echo "❌ ERROR: Decoded .p8 file is empty!"
            exit 1
          fi
          
          # Vérifier le format du fichier .p8
          if ! head -1 "${p8_path}" | grep -q "BEGIN PRIVATE KEY"; then
            echo "❌ ERROR: .p8 file format is invalid (should start with '-----BEGIN PRIVATE KEY-----')"
            echo "First 100 chars: $(head -c 100 "${p8_path}")"
            exit 1
          fi
          
          if ! tail -1 "${p8_path}" | grep -q "END PRIVATE KEY"; then
            echo "❌ ERROR: .p8 file format is invalid (should end with '-----END PRIVATE KEY-----')"
            exit 1
          fi
          
          echo "✅ .p8 file decoded and validated successfully"

          # 4) Créer api_key.json pour fastlane (key_id, issuer_id, key)
          # Utiliser Python pour créer le JSON correctement échappé
          echo "📝 Creating api_key.json..."
          python3 <<PY
          import json
          import os
          from pathlib import Path

          key_id = os.environ['APP_STORE_CONNECT_KEY_ID']
          issuer_id = os.environ['APP_STORE_CONNECT_ISSUER_ID']
          p8_path = Path(f'ios/fastlane/keys/AuthKey_{key_id}.p8')
          
          # Lire la clé privée et nettoyer (supprimer retours à la ligne en fin de fichier)
          key_content = p8_path.read_text(encoding='utf-8').strip()
          
          # Vérifier que la clé commence et se termine correctement
          if not key_content.startswith('-----BEGIN PRIVATE KEY-----'):
              raise ValueError("Invalid .p8 file: missing BEGIN PRIVATE KEY header")
          if not key_content.endswith('-----END PRIVATE KEY-----'):
              raise ValueError("Invalid .p8 file: missing END PRIVATE KEY footer")
          
          # Créer le dictionnaire JSON
          api_key_data = {
              "key_id": key_id,
              "issuer_id": issuer_id,
              "key": key_content
          }
          
          # Écrire le fichier JSON avec échappement correct
          api_key_path = Path('ios/fastlane/keys/api_key.json')
          with open(api_key_path, 'w', encoding='utf-8') as f:
              json.dump(api_key_data, f, indent=2, ensure_ascii=False)
          
          print("✅ api_key.json created successfully")
          PY
          
          if [ $? -ne 0 ]; then
            echo "❌ ERROR: Failed to create api_key.json"
            exit 1
          fi

          echo "✅ API Key files created"
          
          # Vérifier que les fichiers sont créés correctement
          if [ ! -f "${p8_path}" ]; then
            echo "❌ ERROR: .p8 file not created at ${p8_path}"
            exit 1
          fi
          
          if [ ! -f "ios/fastlane/keys/api_key.json" ]; then
            echo "❌ ERROR: api_key.json not created"
            exit 1
          fi
          
          # Vérifier le format JSON
          if ! python3 -m json.tool ios/fastlane/keys/api_key.json > /dev/null 2>&1; then
            echo "❌ ERROR: api_key.json is not valid JSON"
            echo "Content (first 500 chars):"
            head -c 500 ios/fastlane/keys/api_key.json
            exit 1
          fi
          
          echo "✅ Files validated successfully"

      # ------------------------------
      # Validation des credentials App Store Connect
      # ------------------------------
      - name: 🔍 Validate App Store Connect API Key
        working-directory: ios
        shell: bash
        run: |
          set -euo pipefail
          
          echo "🔍 Validating App Store Connect API Key..."
          
          # Vérifier que le fichier api_key.json existe
          if [ ! -f "fastlane/keys/api_key.json" ]; then
            echo "❌ ERROR: api_key.json not found"
            exit 1
          fi
          
          # Afficher les informations (sans la clé complète pour la sécurité)
          echo "📋 API Key Info:"
          python3 <<PY
          import json
          from pathlib import Path
          
          api_key_path = Path('fastlane/keys/api_key.json')
          with open(api_key_path, 'r') as f:
              data = json.load(f)
          
          print(f"  Key ID: {data.get('key_id', 'MISSING')}")
          print(f"  Issuer ID: {data.get('issuer_id', 'MISSING')}")
          key_preview = data.get('key', '')[:50] if data.get('key') else 'MISSING'
          print(f"  Key preview: {key_preview}...")
          PY
          
          # Vérifier le format du fichier .p8
          echo "🔍 Validating .p8 file format..."
          key_id=$(python3 -c "import json; print(json.load(open('fastlane/keys/api_key.json'))['key_id'])")
          p8_file="fastlane/keys/AuthKey_${key_id}.p8"
          
          if [ ! -f "${p8_file}" ]; then
            echo "❌ ERROR: .p8 file not found: ${p8_file}"
            exit 1
          fi
          
          # Vérifier que le fichier .p8 commence par la bonne entête
          if ! head -1 "${p8_file}" | grep -q "BEGIN PRIVATE KEY"; then
            echo "❌ ERROR: .p8 file format is invalid (should start with '-----BEGIN PRIVATE KEY-----')"
            echo "First line: $(head -1 "${p8_file}")"
            exit 1
          fi
          
          # Vérifier que le fichier .p8 se termine correctement
          if ! tail -1 "${p8_file}" | grep -q "END PRIVATE KEY"; then
            echo "❌ ERROR: .p8 file format is invalid (should end with '-----END PRIVATE KEY-----')"
            exit 1
          fi
          
          echo "✅ .p8 file format is valid"
          
          # Tester l'authentification avec fastlane spaceship
          echo "🔐 Testing authentication with App Store Connect API..."
          echo "   This will attempt to connect to App Store Connect..."
          
          # Créer un script Ruby temporaire pour tester l'authentification
          cat > /tmp/test_auth.rb <<'RUBY_SCRIPT'
          require 'spaceship'
          
          api_key_path = 'fastlane/keys/api_key.json'
          
          begin
            # Charger la clé API
            api_key = Spaceship::ConnectAPI::Token.from_json_file(api_key_path)
            
            # Tester l'authentification en récupérant les apps
            Spaceship::ConnectAPI.token = api_key
            apps = Spaceship::ConnectAPI::App.all
            
            puts "✅ Authentication successful!"
            puts "   Found #{apps.length} app(s) in App Store Connect"
            if apps.length > 0
              puts "   First app: #{apps.first.name} (#{apps.first.bundle_id})"
            end
          rescue => e
            puts "❌ ERROR: Authentication failed"
            puts "   Error: #{e.message}"
            puts ""
            puts "   Possible causes:"
            puts "   1. API key is expired or revoked"
            puts "   2. .p8 file is corrupted or incorrectly encoded"
            puts "   3. Key ID or Issuer ID is incorrect"
            puts "   4. API key doesn't have required permissions"
            exit 1
          end
          RUBY_SCRIPT
          
          if bundle exec ruby /tmp/test_auth.rb; then
            echo "✅ App Store Connect API authentication test passed"
          else
            echo ""
            echo "❌ CRITICAL: App Store Connect authentication failed"
            echo "   Please verify your API key in App Store Connect:"
            echo "   https://appstoreconnect.apple.com/access/api"
            exit 1
          fi
          
          rm -f /tmp/test_auth.rb

      # ------------------------------
      # Matchfile (configuration de fastlane match)
      # ------------------------------
      - name: 📝 Create Matchfile (API Key only)
        shell: bash
        run: |
          set -euo pipefail

          cat > ios/fastlane/Matchfile <<EOF
          git_url("${{ secrets.MATCH_GIT_URL }}")
          storage_mode("git")
          type("appstore")
          app_identifier(["com.mayaconnect.app"])
          api_key_path("fastlane/keys/api_key.json")

      # ------------------------------
      # Diagnostic de l'authentification Git
      # ------------------------------
      - name: 🔍 Test Git Authentication
        shell: bash
        env:
          MATCH_GIT_BASIC_AUTHORIZATION: ${{ secrets.MATCH_GIT_BASIC_AUTHORIZATION }}
          MATCH_GIT_URL: ${{ secrets.MATCH_GIT_URL }}
        run: |
          set -euo pipefail

          echo "🔍 Testing Git authentication..."
          echo "Repository URL: ${MATCH_GIT_URL}"
          
          # Vérifier que les secrets sont définis
          if [ -z "${MATCH_GIT_URL}" ]; then
            echo "❌ ERROR: MATCH_GIT_URL is not set"
            exit 1
          fi
          
          if [ -z "${MATCH_GIT_BASIC_AUTHORIZATION}" ]; then
            echo "❌ ERROR: MATCH_GIT_BASIC_AUTHORIZATION is not set"
            exit 1
          fi
          
          # Décoder et vérifier le format de l'authentification
          echo "🔐 Decoding authentication (first 20 chars only for security)..."
          DECODED=$(echo "${MATCH_GIT_BASIC_AUTHORIZATION}" | base64 -d 2>/dev/null || echo "INVALID_BASE64")
          if [ "${DECODED}" = "INVALID_BASE64" ]; then
            echo "❌ ERROR: MATCH_GIT_BASIC_AUTHORIZATION is not valid base64"
            exit 1
          fi
          
          # Vérifier le format username:token
          if [[ ! "${DECODED}" =~ ^[^:]+:.+$ ]]; then
            echo "❌ ERROR: Authentication format should be 'username:token', got: ${DECODED:0:20}..."
            exit 1
          fi
          
          USERNAME=$(echo "${DECODED}" | cut -d: -f1)
          echo "✅ Username detected: ${USERNAME}"
          echo "✅ Authentication format is correct"
          
          # Tester l'accès au repository
          echo "🔍 Testing repository access..."
          TEMP_DIR=$(mktemp -d)
          trap "rm -rf ${TEMP_DIR}" EXIT
          
          if git clone \
            -c http.extraheader="Authorization: Basic ${MATCH_GIT_BASIC_AUTHORIZATION}" \
            "${MATCH_GIT_URL}" \
            "${TEMP_DIR}/test-clone" \
            2>&1; then
            echo "✅ Repository access successful!"
            rm -rf "${TEMP_DIR}"
          else
            echo "❌ ERROR: Cannot access repository"
            echo "Please check:"
            echo "  1. Repository exists and is private"
            echo "  2. PAT token has 'repo' scope"
            echo "  3. PAT token has access to this repository"
            echo "  4. Username in base64 matches your GitHub username"
            exit 1
          fi

      # ------------------------------
      # Initialisation de match
      # ------------------------------
      - name: 🎫 Initialize Match and Create Certificates (API Key)
        working-directory: ios
        shell: bash
        env:
          MATCH_PASSWORD: ${{ secrets.MATCH_PASSWORD }}
          MATCH_GIT_BASIC_AUTHORIZATION: ${{ secrets.MATCH_GIT_BASIC_AUTHORIZATION }}
          MATCH_GIT_URL: ${{ secrets.MATCH_GIT_URL }}
        run: |
          set -euo pipefail

          echo "🔐 Initializing Match with App Store Connect API Key..."
          bundle exec fastlane match appstore --verbose --force --skip_certificate_matching

          echo "✅ Match initialized successfully!"
          echo "✅ Certificates and profiles stored in: ${{ secrets.MATCH_GIT_URL }}"

      # ------------------------------
      # Résumé
      # ------------------------------
      - name: ✅ Success - Match is ready!
        shell: bash
        run: |
          set -euo pipefail

          echo "======================================"
          echo "🎉 MATCH SETUP COMPLETED!"
          echo "======================================"
          echo ""
          echo "✅ iOS Certificates created"
          echo "✅ Provisioning Profiles created"
          echo "✅ Everything stored in: ${{ secrets.MATCH_GIT_URL }}"
          echo ""
          echo "📋 Next steps:"
          echo "1. The deploy workflow can now build iOS"
          echo "2. You can disable or delete this workflow"
          echo ""

